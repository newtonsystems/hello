# Docker Compose YML Config

# This is the Development Local Config (Docker Compose v1.6.0 up)


#Scenarios
## a full local kubernetes (all services)
## empty env with just infra (linkerd, namerd, db etc)
## a kubernetes (linkerd --> the branch's env)
## stage to prod type environemnt 

version: '3'
services:
    app:
      build: 
        context: ./
      tty:
        true
      volumes:
        - ./app:/app
        - ../../libutils:/usr/local/src/libutils
        - ./wheelhouse:/wheelhouse
      external_links:
        - linkerd

      #links:
      #  - postgres:postgres
      #   - redis:redis
      #   - rabbitmq:rabbitmq
      #depends_on:
      #  - postgres
      #   - redis
      #   - rabbitmq
      env_file:
         - ./config/env-local

    # redis:
    #   extends:
    #     file: config/docker-compose-common.yml
    #     service: redis
    #   env_file:
    #     - ./config/env-local

    # postgres:
    #   extends:
    #     file: config/docker-compose-common.yml
    #     service: postgres
    #   env_file:
    #     - ./config/env-local

    # rabbitmq:
    #   extends:
    #     file: config/docker-compose-common.yml
    #     service: rabbitmq
    #   env_file:
    #     - ./config/env-local

    # nginx:
    #   extends:
    #     file: config/docker-compose-common.yml
    #     service: nginx
    #   links:
    #     - app:app
    #   volumes_from:
    #     - app
    #   env_file:
    #     - ./config/env-local

